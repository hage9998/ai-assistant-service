apiVersion: v1
kind: Service
metadata:
  name: ai-assistant-api-service
  namespace: life-gamefication-dev
spec:
  selector:
    app: ai-assistant-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30009
  type: NodePort
