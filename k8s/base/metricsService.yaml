apiVersion: v1
kind: Service

metadata:
  name: auth-api-metrics
  namespace: life-gamefication-dev
  labels:
    app: auth-api

spec:
  selector:
    app: auth-api

  ports:
    - name: metrics
      port: 8889
      targetPort: 8889
